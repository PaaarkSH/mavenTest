
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoardMapper">
    <insert id="insertBoard" parameterType="com.biz.board.BoardVO">
        insert into board(seq, title, writer, content)
        values (#{seq}, #{title}, #{writer}, #{content} )
    </insert>

    <update id="updateBoard" parameterType="com.biz.board.BoardVO">
        update board
        set title = #{title}
            , writer = #{writer}
            , content = #{content}
        where seq = #{seq}

    </update>

    <delete id="deleteBoard" parameterType="com.biz.board.BoardVO">
        delete board
        where seq = #{seq}
    </delete>

    <select id="getBoard" parameterType="com.biz.board.BoardVO" resultType="com.biz.board.BoardVO">
        select seq, title, writer, content, regdate, cnt
        from board
        where seq = #{seq}
    </select>

    <select id="getBoardList" parameterType="com.biz.board.BoardVO">
        select seq, title, writer, content, regdate, cnt
        from board
        order by seq
    </select>
</mapper>
<!-- https://mybatis.org/mybatis-3/ko/getting-started.html -->
